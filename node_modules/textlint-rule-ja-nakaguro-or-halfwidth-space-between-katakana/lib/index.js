// LICENSE : MIT
"use strict";

/*
 カタカナ語間のスペースの有無
 中黒または半角スペースを用いてカタカナ語を区切ります。
 */
var _textlintRuleHelper = require("textlint-rule-helper");
module.exports = function (context) {
  var {
    Syntax,
    RuleError,
    report,
    getSource
  } = context;
  var helper = new _textlintRuleHelper.RuleHelper();
  return {
    [Syntax.Str](node) {
      if (!helper.isPlainStrNode(node)) {
        return;
      }
      var text = getSource(node);
      // カタカナ(カタカナ以外)カタカナ のパターンを取り出す
      for (var match of text.matchAll(/[ァ-ヶー]([^[ァ-ヶー])[ァ-ヶー]/g)) {
        // カタカナの間を全角スペースでは区切らない
        var _text = match[1];
        if (_text === "　") {
          report(node, new RuleError("カタカナ語間は中黒（・）または半角スペースを用いてカタカナ語を区切ります", {
            index: match.index + 1
          }));
        }
      }
    }
  };
};
//# sourceMappingURL=index.js.map